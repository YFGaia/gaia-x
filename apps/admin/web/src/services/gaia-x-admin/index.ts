// @ts-ignore
/* eslint-disable */
// API 更新时间：
// API 唯一标识：
import * as api from './api';
import * as initdb from './initdb';

export default {
  api,
  initdb,
};

// 导出所有API函数，方便直接导入使用
export * from './api';
